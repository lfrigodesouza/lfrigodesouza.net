import React from 'react';
import ReactDOM from 'react-dom';
import Theme from './providers/ThemeProvider/Theme';

ReactDOM.render(
  <React.StrictMode>
    <Theme>LFrigoDeSouza.NET</Theme>
  </React.StrictMode>,
  document.getElementById('root'),
);
